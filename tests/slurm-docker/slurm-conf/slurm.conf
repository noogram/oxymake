# slurm.conf — Minimal SLURM configuration for OxyMake demo cluster
#
# 2-node cluster (c1, c2) with a single "normal" partition.

ClusterName=oxymake-demo
SlurmctldHost=slurmctld

# Logging
SlurmctldLogFile=/var/log/slurm/slurmctld.log
SlurmdLogFile=/var/log/slurm/slurmd.log

# Scheduling
SchedulerType=sched/backfill
SelectType=select/cons_tres
SelectTypeParameters=CR_Core

# Accounting
AccountingStorageType=accounting_storage/slurmdbd
AccountingStorageHost=slurmdbd
JobAcctGatherType=jobacct_gather/linux

# Process tracking
ProctrackType=proctrack/linuxproc

# Cgroup — disabled for Docker compatibility
TaskPlugin=task/none

# Timeouts
SlurmctldTimeout=60
SlurmdTimeout=60
InactiveLimit=0
MinJobAge=300
WaitTime=0

# Paths
StateSaveLocation=/var/spool/slurmctld
SlurmdSpoolDir=/var/spool/slurmd
SlurmctldPidFile=/var/run/slurm/slurmctld.pid
SlurmdPidFile=/var/run/slurm/slurmd.pid

# Auth
AuthType=auth/munge

# User
SlurmUser=slurm

# Nodes
NodeName=c[1-2] CPUs=2 RealMemory=2048 State=UNKNOWN

# Partitions
PartitionName=normal Nodes=c[1-2] Default=YES MaxTime=INFINITE State=UP

#!/bin/bash
# Docker SLURM shim: scancel
#
# Proxies scancel commands to the Docker SLURM cluster.
#
# Environment:
#   SLURM_DOCKER_CONTAINER  Container name (default: slurmctld)

set -euo pipefail

CONTAINER="${SLURM_DOCKER_CONTAINER:-slurmctld}"
exec docker exec "$CONTAINER" scancel "$@"
